import 'package:flutter/cupertino.dart';

class SearchPageView extends StatelessWidget {
  const SearchPageView({super.key, required bool isEmpty});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
